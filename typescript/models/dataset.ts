// Generated by ts-to-zod
import { z } from "zod";

export const table1ResourceSchema = z.object({
    name: z.literal("table1"),
    tableSchema: z.literal("https://fairspec.github.io/schemas/0.1.1/table1.json")
});

export const table2ResourceSchema = z.object({
    name: z.literal("table2"),
    tableSchema: z.literal("https://fairspec.github.io/schemas/0.1.1/table2.json")
});

export const resourceSchema = z.union([table1ResourceSchema, table2ResourceSchema]);

export const datasetSchema = z.object({
    $schema: z.literal("https://fairspec.github.io/profiles/0.1.1/dataset.json"),
    resources: z.tuple([resourceSchema]).rest(resourceSchema)
});

export const fairspecExtensionProfileSchema = datasetSchema;
