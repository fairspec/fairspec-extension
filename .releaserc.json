{
  "branches": ["main"],
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    [
      "@semantic-release/release-notes-generator",
      { "preset": "conventionalcommits" }
    ],
    [
      "@semantic-release/exec",
      { "prepareCmd": "pnpm /vers/ ${nextRelease.version}" }
    ],
    ["@semantic-release/exec", { "prepareCmd": "pnpm generate" }],
    [
      "@semantic-release/git",
      {
        "message": "release: v${nextRelease.version}",
        "assets": [
          "python/pyproject.toml",
          "python/*/models/*.py",
          "typescript/package.json",
          "typescript/models/*.ts",
          "docs/profiles/*.json",
          "docs/specification/metadata.md",
          "docs/specification/data/*.md",
          ".livemark/public/profiles/*/*.json",
          ".livemark/public/schemas/*/*.json",
          "package.json"
        ]
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "pnpm /build/",
        "publishCmd": "pnpm /release/"
      }
    ],
    ["@semantic-release/github"]
  ]
}
